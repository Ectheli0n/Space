package application;

import javafx.scene.paint.Color;
import javafx.scene.shape.Rectangle;

public class Laser extends Rectangle{
	
	public Laser(double w, double h, Color color) {
		super (w, h, color);
	}
}
